# ============================================================
# Backend — Production Environment
# Set these as environment variables on your hosting platform
# (Railway, Render, Fly.io, Docker, etc.).
# Never commit this file with real values.
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# ── CORS ─────────────────────────────────────────────────────
# Production frontend domain only — no localhost
CORS_ALLOWED_ORIGINS=https://sentrylabs.live

# ── SDK ──────────────────────────────────────────────────────
# Your production frontend URL (used to build the OAuth link for the CLI)
SDK_VERIFICATION_BASE_URL=https://sentrylabs.live

# Your production backend URL (returned to the SDK after device approval)
SDK_DEFAULT_DSN=https://api.sentrylabs.live

# Crash on startup if any required DB table is missing
SDK_SCHEMA_STRICT_STARTUP=true

# ── Risk Notifications ────────────────────────────────────────
NOTIFICATION_COOLDOWN_SECONDS=300
RISK_ALERT_WEBHOOK_URL=

# ── Email Alerts ─────────────────────────────────────────────
# Sender address — must be authorised by your SMTP provider
EMAIL_ALERT_FROM=alerts@sentrylabs.live

# Comma-separated fallback recipients.
# Leave empty to use each app owner's Google account email instead.
EMAIL_ALERT_TO=

# Example: Gmail SMTP via App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=alerts@sentrylabs.live
SMTP_PASSWORD=<app-password>
SMTP_USE_TLS=true
